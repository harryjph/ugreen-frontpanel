# leds-mcu probes the panel MCU over I2C during init; make sure the
# SMBus controller driver exists first (else its adapter scan finds nothing).
softdep leds-mcu pre: i2c_i801 i2c_designware_platform i2c_designware_core
